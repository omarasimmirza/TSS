spring.datasource.hikari.connection-test-query=SELECT 1 FROM DUAL
spring.datasource.hikari.connection-timeout=60000
spring.datasource.hikari.maximum-pool-size=500
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.minimum-idle=20
spring.datasource.hikari.validation-timeout=3000
spring.datasource.hikari.idle-timeout=60000

#locally running.
# spring.datasource.url = jdbc:mysql://localhost:3306/URSTechSupport?useSSL=false
# spring.datasource.username = asim
# spring.datasource.password = asimmirza
# spring.profiles.active=development
# server.servlet.context-path=/paradigmSoft

spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name= com.mysql.cj.jdbc.Driver
#following line is working takes care of public key retrieval not allowed error.
spring.datasource.url=jdbc:mysql://${DB_HOST}/${DB_NAME}?allowPublicKeyRetrieval=true&useSSL=false 
server.servlet.context-path=/paradigmSoft
